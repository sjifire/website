{
  "name": "SJIFIRE",
  "version": "1.0.0",
  "description": "",
  "main": ".eleventy.js",
  "scripts": {
    "watch:eleventy": "eleventy --serve",
    "build:eleventy": "eleventy",
    "start": "npm-run-all clean watch:*",
    "build": "npm-run-all clean build:*",
    "clean": "rimraf public"
  },
  "dependencies": {
    "@11ty/eleventy": "^0.12.1",
    "@netlify/plugin-sitemap": "^v0.8",
    "@netlify/plugin-lighthouse": "^v2.1",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "clean-css": "^5.1.3",
    "fs": "^0.0.1-security",
    "html-minifier": "^4.0.0",
    "npm-run-all": "^4.1.5",
    "path": "^0.12.7",
    "rimraf": "^3.0.2"
  },
  "engines": {
    "node": "16.x"
  }
}
