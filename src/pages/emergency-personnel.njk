---
title: "Emergency Personnel"
permalink: "/about/emergency-personnel/index.html"
---

{% extends "default_single_col_page.njk" %}
{% set pageData = emergency_personnel %}

{% block content %}
  <div class="l-squeezed block-content">
    <h1>
      {{ title | safe }}
    </h1>
    <div class="lede">
      {{ pageData.preamble | markdownify | safe }}
    </div>
    {% set staffOnly = pageData.personnel | pluckByValue('staff', 'staff_type') %}
    {% set volunteersOnly = pageData.personnel | pluckByValue('volunteer', 'staff_type') %}
    <section class="personnel-section">
      <h2>Staff</h2>
      <div class="l-grid l-grid--personnel">
        {% for person in staffOnly %}
          <div class="person">
            <div class="person__img{{ ' noimg' if not person.img_file }}">
              {% if person.img_file %}
                <img src="{%- imgPath '/assets/personnel_imgs/' + person.img_file, 'f_auto,q_auto:good,c_fill,w_315,h_328,g_face' -%}" alt="Headshot of {{ person.first_name }} {{ person.last_name }}" width="157.5" height="164">
              {% else %}
                <div>
                  <svg><use xlink:href='#profile' /></svg>
                </div>
              {% endif %}
            </div>
            <h3>{{ person.first_name }} {{ person.last_name }}</h3>
            {% if person.title %}<h4>{{ person.title }}</h4>{% endif %}
            {% if person.rank %}<h5>{{ person.rank }}</h5>{% endif %}
            <ul>
              {% for role in person.roles %}
                <li>{{ role }}</li>
              {% endfor %}
            </ul>
          </div>
        {% endfor %}
      </div>
    </section>
    <section class="personnel-section">
      <h2>Volunteers</h2>
      <div class="l-grid l-grid--personnel">
        {% for person in volunteersOnly | sort(attribute='last_name') %}
          <div class="person">
            <div class="person__img{{ ' noimg' if not person.img_file }}">
              {% if person.img_file %}
                <img src="{%- imgPath '/assets/personnel_imgs/' + person.img_file, 'f_auto,q_auto:good,c_fill,w_315,h_328,g_face' -%}" alt="Headshot of {{ person.first_name }} {{ person.last_name }}" width="157.5" height="164">
              {% else %}
                <div>
                  <svg><use xlink:href='#profile' /></svg>
                </div>
              {% endif %}
            </div>
            <h3>{{ person.first_name }} {{ person.last_name }}</h3>
            {% if person.title %}<h4>{{ person.title }}</h4>{% endif %}
            {% if person.rank %}<h5>{{ person.rank }}</h5>{% endif %}
            <ul>
              {% for role in person.roles %}
                <li>{{ role }}</li>
              {% endfor %}
            </ul>
          </div>
        {% endfor %}
      </div>
    </section>
  </div>
{% endblock %}
