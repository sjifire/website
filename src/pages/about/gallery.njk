---
title: "Photo Gallery"
nav_order: 8
layout: false
---
{% extends "base.njk" %}

{% block content %}
<div class="l-squeezed block-content">
  <h1>{{ title }}</h1>
  <p class="gallery-intro">A glimpse into the people, equipment, and community events that make up San Juan Island Fire & Rescue.</p>

  <div class="photo-gallery">
    {% for img in homepage.carousel_images %}
      <button type="button" class="photo-gallery__item" data-index="{{ loop.index0 }}" aria-label="View {{ img.alt }}">
        <img
          class="photo-gallery__thumb leave-alone"
          src="{%- imgPath img.src | safe, 'f_auto,q_auto:good,c_fill,w_400,h_300' -%}"
          alt="{{ img.alt }}"
          loading="lazy"
        >
        <span class="photo-gallery__caption">{{ img.alt }}</span>
      </button>
    {% endfor %}
  </div>
</div>

<!-- Lightbox Modal -->
<div class="lightbox" id="lightbox" role="dialog" aria-modal="true" aria-label="Image lightbox">
  <button type="button" class="lightbox__close" aria-label="Close lightbox">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <line x1="18" y1="6" x2="6" y2="18"></line>
      <line x1="6" y1="6" x2="18" y2="18"></line>
    </svg>
  </button>
  <button type="button" class="lightbox__nav lightbox__nav--prev" aria-label="Previous image">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <polyline points="15 18 9 12 15 6"></polyline>
    </svg>
  </button>
  <button type="button" class="lightbox__nav lightbox__nav--next" aria-label="Next image">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <polyline points="9 6 15 12 9 18"></polyline>
    </svg>
  </button>
  <figure class="lightbox__content">
    <img class="lightbox__image leave-alone" src="" alt="">
    <figcaption class="lightbox__caption"></figcaption>
  </figure>
</div>

<script>
  window.galleryImages = [
    {% for img in homepage.carousel_images %}
    {
      src: "{%- imgPath img.src | safe, 'f_auto,q_auto:good,c_limit,w_1400,h_1000' -%}",
      alt: "{{ img.alt }}"
    }{% if not loop.last %},{% endif %}
    {% endfor %}
  ];
</script>
{% endblock %}

{% block scripts %}
<script src="/js/gallery.js" defer></script>
{% endblock %}
