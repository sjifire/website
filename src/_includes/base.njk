<!DOCTYPE html>
<html lang="en">
<head>
  {# preload font? #}
  <meta charset="utf-8">
  <meta name="format-detection" content="telephone=no">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>{{ site.name if page.url === '/' else title + ' | ' + site.name }}</title>
  {# meta-desc #}
  {# icon tags #}
    <link rel="icon" type="image/png" href="{{ 'https://res.cloudinary.com/san-juan-fire-district-3/image/fetch/f_auto/' + site.rootUrl if helpers.env === 'production' }}/assets/sjifire-logo-clear.png">
  {# og #}
  {#
    <link rel="preconnect" href="//www.google-analytics.com">
    <link rel="dns-prefetch" href="//www.google-analytics.com">
    <link rel="preconnect" href="//www.datocms-assets.com/">
    <link rel="dns-prefetch" href="//www.datocms-assets.com/">
  #}
  {%- set css -%}
    {% include "site.css" %}
  {%- endset -%}
  <style>
    {{ css | cssmin | safe }}
  </style>
</head>
<body>

  {% include "svg-sprite.svg" %}

  <header class="site-header" role="banner">
    {% include "header.njk" %}
  </header>

  <header class="page-header">
    <div class="rap">
      <article class="page-header__copy">
        {% block page_header %}{% endblock %}
      </article>
    </div>
  </header>

  {% include "page_hero.njk" %}

  <main class="site-main" role="main">
    <div class="rap">
      {% block content %}
        {{ content | safe }}
      {% endblock %}
    </div>
  </main>

  <footer class="site-footer" role="contentinfo">
    <div class="rap">
      {% include "footer.njk" %}
    </div>
  </footer>

  {% block script %}{% endblock %}
</body>
</html>