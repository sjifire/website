<!DOCTYPE html>
<html lang="en">
<head>
  {# preload font? #}
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>{{ site.name if page.url === '/' else title + ' | ' + site.name }}</title>
  {# meta-desc #}
  {# icon tags #}
  {# og #}
  {#
    <link rel="preconnect" href="//www.google-analytics.com">
    <link rel="dns-prefetch" href="//www.google-analytics.com">
    <link rel="preconnect" href="//www.datocms-assets.com/">
    <link rel="dns-prefetch" href="//www.datocms-assets.com/">
  #}
  {%- set css -%}
    {% include "site.css" %}
  {%- endset -%}
  <style>
    {{ css | cssmin | safe }}
  </style>
</head>
<body>
  <header class="site-header" role="banner">
    {% include "header.njk" %}
  </header>

  <header class="page-header">
    <div class="rap">
      {% if page.url === '/' %}
        <h1>
          {{ title }}
        </h1>
        <p>
          {{ homepage.preamble }}
        </p>
      {% elif page.fileSlug === 'posts-permalink' %}
        <h2>
          {{ post.post_type }}{{ ': ' + post.date | postDateTerseISO if post.post_type === 'Advisory' }}
        </h2>
      {% else %}
        <h1>
          {{ title }}
        </h1>
      {% endif %}
    </div>
  </header>

  <main class="site-main" role="main">
    <div class="rap">
      {% block content %}
        {{ content | safe }}
      {% endblock %}
    </div>
  </main>

  <footer class="site-footer" role="contentinfo">
    <div class="rap">
      {% include "footer.njk" %}
    </div>
  </footer>

  {% block script %}{% endblock %}
</body>
</html>